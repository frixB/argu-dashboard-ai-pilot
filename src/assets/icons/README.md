# Icons

Empty on purpose. Icon assets exported from Figma
(`get_design_context` returns them as short-lived `figma.com/api/mcp/asset/...`
URLs) should be downloaded and committed here once the full Dashboard/Main
screen is implemented — see `docs/ARCHITECTURE.md`, unresolved decision #3.
